#include <stdio.h>
#include <stddef.h>
#include <sys/_types/_pid_t.h>
#include "tools.h"

pid_t
get_main_pid() {
    FILE* f = fopen("data/main.pid", "r");
    fscanf("", const char *restrict, ...)
}

int
main(int argc, char** argv) {
    if (argc != 2) return -1;

    int client_cnt = atoi(argv[1]);

    

    
}

